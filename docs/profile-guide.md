The `src/profile.py` file contains the intern profile.

The profile contains:

- Intern name
- Role
- Department
- Skills
- Function to print the profile

The function used to display the profile is:

`print_profile()`